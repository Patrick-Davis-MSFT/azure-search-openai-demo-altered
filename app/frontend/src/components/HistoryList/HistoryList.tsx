import { Text } from "@fluentui/react";
import { History24Regular } from "@fluentui/react-icons";

import styles from "./HistoryList.module.css";

interface Props {
    className?: string;
}

export const HistoryList = ({ className }: Props) => {
    return (
        <><span>some List Here</span></>
    );
};
